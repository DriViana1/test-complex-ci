name: CI

on:
  push:
    branches:
      - main
      - feature/github-actions-ci

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker version
        run: docker --version
      
      - name: Build client test image
        run: docker build -t react-test -f ./client/Dockerfile.dev ./client

      - name: Run client tests
        run: docker run react-test npm test -- --coverage --watchAll=false

      - name: Build client image
        run: docker build -t multi-client ./client

      - name: Build nginx image
        run: docker build -t multi-nginx ./nginx

      - name: Build server
        run: docker build -t multi-server -f ./server/Dockerfile.dev ./server

      - name: Build worker
        run: docker build -t multi-worker -f ./worker/Dockerfile.dev ./worker
        #teste